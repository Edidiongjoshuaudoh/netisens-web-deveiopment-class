let randomNumbers = [];

console.log(randomNumber)

for(let i = 1; i <= 20; i++){
    // generate a random number between 0 and 0..
    let randomNumbers = Math.random();
    
    // multiply them by 30 to nake them greater
    randomNumbers = randomNumbers * 30;
    // console.log(randomNumber);

    // convert them to integers
    randomNumbers = Math.round(randomNumber);
    // console.log(randomNumber)

    randomNumbers.push(randomNumber);

    // seperate even numbers from odd numbers

// for(let number of randomNumber){
//         if(number % 2 === 0){
//             eve    nNumbers.push(number)
//         }else
//     }
}